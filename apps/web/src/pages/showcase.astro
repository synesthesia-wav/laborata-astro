---
import "@workspace/ui/globals.css";
import { BRANCHES } from "@workspace/data/branches";
import { TESTS } from "@workspace/data/canonical";
import { getPricesForId } from "@workspace/data/prices";
import {
  Alert,
  AlertDescription,
  AlertTitle,
} from "@workspace/ui/components/alert";
import { AspectRatio } from "@workspace/ui/components/aspect-ratio";
import { Badge } from "@workspace/ui/components/badge";
import { Button } from "@workspace/ui/components/button";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@workspace/ui/components/card";
import {
  Empty,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "@workspace/ui/components/empty";
import { Separator } from "@workspace/ui/components/separator";
import { Skeleton } from "@workspace/ui/components/skeleton";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@workspace/ui/components/table";
import { AnalizeBrowse } from "../components/analize/AnalizeBrowse";
import { BranchesExplorer } from "../components/branches/branches-explorer";
import { BundlesShelf } from "../components/home/bundles-shelf";
import { CatalogShelf } from "../components/home/catalog-shelf";
import { ConcernsRow } from "../components/home/concerns-row";
import { HeroSearch } from "../components/home/hero-search";
import { LabsTeaser } from "../components/home/labs-teaser";
import {
  PRICE_OFFERS_LARGE,
  PRICE_OFFERS_SINGLE,
  PRICE_OFFERS_VARIANT,
  PRODUCT_GALLERY,
} from "../components/product/data";
import { Faq } from "../components/product/faq";
import { PriceComparison } from "../components/product/price-comparison";
import { ProductGallery } from "../components/product/product-gallery";
import { ProductHeader } from "../components/product/product-header";
import { ProductSpecs } from "../components/product/product-specs";
import { BundleDetail } from "../components/profil/BundleDetail";

const _labLabel: Record<string, string> = {
  alfa: "Alfa",
  invitro: "Invitro",
  medexpert: "MedExpert",
  sante: "Sante",
  synevo: "Synevo",
};
const _rawB12 = getPricesForId("vitamina-b12");
const PRICE_OFFERS_REAL_B12 = (_rawB12 ?? []).map((o) => ({
  lab: _labLabel[o.vendor] ?? o.vendor,
  price_mdl: o.price_mdl,
  sourceUrl: o.sourceUrl,
  variant: o.variant,
  vendor: o.vendor,
}));
---

<html lang="ro">
  <head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <meta
      content="Showcase — stări Laborata PDP Vitamina B12"
      name="description"
    >
    <meta content="noindex, nofollow" name="robots">
    <link href="/favicon.svg" rel="icon" type="image/svg+xml">
    <title>Laborata — Showcase</title>
  </head>
  <body class="bg-background text-foreground antialiased">
    <header class="sticky top-0 z-10 border-b bg-background/80 backdrop-blur">
      <div
        class="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-3 sm:px-6"
      >
        <div class="min-w-0">
          <h1
            class="truncate font-heading text-sm font-semibold tracking-tight"
          >
            Laborata — Showcase
          </h1>
          <p class="truncate text-xs text-muted-foreground">
            PDP Vitamina B12 — minimal · 5 secțiuni · Paper assets · price truth
            W2 · 7033 routes (417 comparison + 6616 catalog_only) + 33 panels +
            145 branches
          </p>
        </div>
        <div class="flex gap-2">
          <a
            class="inline-flex h-8 items-center rounded-full border bg-card px-3 text-xs font-medium hover:bg-muted"
            href="/analize/vitamina-b12"
            >Deschide PDP</a
          >
          <a
            class="inline-flex h-8 items-center rounded-full border bg-card px-3 text-xs font-medium hover:bg-muted"
            href="/"
            >Home</a
          >
        </div>
      </div>
    </header>

    <main
      class="mx-auto flex w-full max-w-7xl min-w-0 flex-col gap-8 p-4 sm:p-6"
    >
      <Card>
        <CardHeader>
          <CardTitle className="text-lg"
            >Matrice stări — PDP minimal (W2 price truth)</CardTitle
          >
          <CardDescription>
            Păstrate 5 secțiuni: galerie 1:1, header single price (lowest +
            priceAsOf + provenance), specs per-vendor onest, comparație prețuri
            single fee (5 labs), FAQ. Fără
            Plan/Trust/Testimoniale/HowItWorks/Interpretation/Related. Fiecare
            instanță are
            <code class="rounded bg-muted px-1 py-0.5 font-mono text-xs"
              >idSuffix</code
            >
            unic — fără duplicate ARIA.
          </CardDescription>
        </CardHeader>
      </Card>

      <section class="flex flex-col gap-4">
        <h2 class="font-heading text-sm font-semibold tracking-tight">
          ProductGallery — 1:1, fără badges
        </h2>
        <div class="grid gap-4 md:grid-cols-3">
          <Card size="sm">
            <CardHeader
              ><CardTitle className="text-sm">Loading</CardTitle
              ><CardDescription
                >Skeleton aspect-square</CardDescription
              ></CardHeader
            >
            <CardContent
              ><ProductGallery
                idSuffix="showcase-loading"
                images={[]}
                loading={true}
                client:load
              /></CardContent
            >
          </Card>
          <Card size="sm">
            <CardHeader
              ><CardTitle className="text-sm">Fără imagine</CardTitle
              ><CardDescription
                >Empty aspect-square</CardDescription
              ></CardHeader
            >
            <CardContent
              ><ProductGallery
                idSuffix="showcase-empty"
                images={[]}
                client:load
              /></CardContent
            >
          </Card>
          <Card size="sm">
            <CardHeader
              ><CardTitle className="text-sm">Una</CardTitle
              ><CardDescription>Single 1:1</CardDescription></CardHeader
            >
            <CardContent
              ><ProductGallery
                idSuffix="showcase-one"
                images={[PRODUCT_GALLERY[0]]}
                client:load
              /></CardContent
            >
          </Card>
        </div>
        <Card>
          <CardHeader
            ><CardTitle className="text-sm"
              >Multe (3) + carousel 1:1</CardTitle
            ></CardHeader
          >
          <CardContent
            ><ProductGallery
              idSuffix="showcase-many"
              images={PRODUCT_GALLERY}
              client:load
            /></CardContent
          >
        </Card>
        <Card>
          <CardHeader
            ><CardTitle className="text-sm"
              >Eroare + retry</CardTitle
            ></CardHeader
          >
          <CardContent className="flex flex-col gap-3">
            <Alert variant="destructive">
              <AlertTitle>Eroare la încărcarea imaginilor</AlertTitle>
              <AlertDescription>Nu am putut încărca galeria.</AlertDescription>
            </Alert>
            <Button size="sm" variant="outline">Reîncearcă</Button>
          </CardContent>
        </Card>
      </section>

      <Separator />

      <section class="flex flex-col gap-4">
        <h2 class="font-heading text-sm font-semibold tracking-tight">
          ProductHeader — lowest price_mdl + priceAsOf + provenance (33 lei demo
          → 175 lei onest)
        </h2>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card
            ><CardHeader
              ><CardTitle className="text-sm">Loading</CardTitle></CardHeader
            ><CardContent
              ><ProductHeader
                idSuffix="showcase-h-loading"
                loading={true}
                client:load
              /></CardContent
            ></Card
          >
          <Card
            ><CardHeader
              ><CardTitle className="text-sm">Disabled</CardTitle
              ><CardDescription>Buton dezactivat</CardDescription></CardHeader
            ><CardContent
              ><ProductHeader
                disabled={true}
                idSuffix="showcase-h-disabled"
                client:load
              /></CardContent
            ></Card
          >
          <Card
            ><CardHeader
              ><CardTitle className="text-sm">Normal — 175 lei sub h1</CardTitle
              ><CardDescription
                >cel mai mic din 4/5</CardDescription
              ></CardHeader
            ><CardContent
              ><ProductHeader
                idSuffix="showcase-h-normal"
                client:load
              /></CardContent
            ></Card
          >
        </div>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card>
            <CardHeader
              ><CardTitle className="text-sm">Permission denied</CardTitle
              ><CardDescription>stare showcase W4</CardDescription></CardHeader
            >
            <CardContent
              ><ProductHeader
                idSuffix="showcase-h-permission"
                permissionDenied={true}
                client:load
              /></CardContent
            >
          </Card>
          <Card>
            <CardHeader
              ><CardTitle className="text-sm">Large 1.250.000 lei</CardTitle
              ><CardDescription>wrapping preț mare</CardDescription></CardHeader
            >
            <CardContent
              ><ProductHeader
                idSuffix="showcase-h-large"
                lowestPrice={1_250_000}
                client:load
              /></CardContent
            >
          </Card>
          <Card>
            <CardHeader
              ><CardTitle className="text-sm">Variant — 24-ore</CardTitle
              ><CardDescription>qualifier</CardDescription></CardHeader
            >
            <CardContent
              ><ProductHeader
                idSuffix="showcase-h-variant"
                variant="24-ore"
                client:load
              /></CardContent
            >
          </Card>
        </div>
        <Card>
          <CardHeader
            ><CardTitle className="text-sm"
              >Text foarte lung / nedespărțit</CardTitle
            ><CardDescription
              >Verifică truncate 1:1 · 320px</CardDescription
            ></CardHeader
          >
          <CardContent className="flex flex-col gap-4">
            <div class="rounded-lg border p-4">
              <h3 class="text-balance font-heading text-xl font-semibold">
                Vitamina B12 (Cobalamină) cu denumire extrem de lungă pentru
                wrapping pe 320px
              </h3>
              <p class="mt-2 break-all text-sm text-muted-foreground">
                SupercalifragilisticexpialidociousVitaminaB12CobalaminăTestDeSângeCuNumeFoarteLungCuVariantCantitativ24Ore
              </p>
              <div class="mt-2 flex flex-wrap gap-2">
                <Badge>999.999 lei</Badge
                ><Badge variant="outline">175 lei</Badge
                ><Badge variant="secondary">1.250.000 lei</Badge>
              </div>
            </div>
            <Alert
              ><AlertTitle>Număr mare</AlertTitle
              ><AlertDescription
                >Preț <span class="font-mono font-medium">1.250.000 lei</span> —
                wrapping preț (ro-RO grouping).</AlertDescription
              ></Alert
            >
          </CardContent>
        </Card>
      </section>

      <Separator />

      <section class="flex flex-col gap-4">
        <h2 class="font-heading text-sm font-semibold tracking-tight">
          ProductSpecs — per-vendor onest (91% specimen 69% method 93% TAT 53%
          protocol 6.9% refs) · Synevo 1 zi · Sante 14 zile
        </h2>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card
            ><CardHeader
              ><CardTitle className="text-sm"
                >Specs — loading</CardTitle
              ></CardHeader
            ><CardContent
              ><ProductSpecs
                idSuffix="showcase-specs-loading"
                loading={true}
                client:load
              /></CardContent
            ></Card
          >
          <Card
            ><CardHeader
              ><CardTitle className="text-sm"
                >Specs — collapsible normal</CardTitle
              ><CardDescription
                >Citește mai mult / mai puțin (honest null)</CardDescription
              ></CardHeader
            ><CardContent
              ><ProductSpecs
                idSuffix="showcase-specs-normal"
                client:load
              /></CardContent
            ></Card
          >
        </div>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card
            ><CardHeader
              ><CardTitle className="text-sm">Specs — disabled</CardTitle
              ><CardDescription>W4 missing state</CardDescription></CardHeader
            ><CardContent
              ><ProductSpecs
                disabled={true}
                idSuffix="showcase-specs-disabled"
                client:load
              /></CardContent
            ></Card
          >
          <Card
            ><CardHeader
              ><CardTitle className="text-sm"
                >Specs — permission denied</CardTitle
              ><CardDescription>W4 missing state</CardDescription></CardHeader
            ><CardContent
              ><ProductSpecs
                idSuffix="showcase-specs-permission"
                permissionDenied={true}
                client:load
              /></CardContent
            ></Card
          >
        </div>
      </section>

      <Separator />

      <section class="flex flex-col gap-4">
        <h2 class="font-heading text-sm font-semibold tracking-tight">
          PriceComparison — single fee line (5 labs Synevo Sante Invitro
          MedExpert Alfa) · plus 30 lei o singură dată / 0 lei — inclus
        </h2>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card
            ><CardHeader
              ><CardTitle className="text-sm">Loading</CardTitle></CardHeader
            ><CardContent
              ><PriceComparison
                idSuffix="pc-loading"
                loading={true}
                client:load
              /></CardContent
            ></Card
          >
          <Card>
            <CardHeader
              ><CardTitle className="text-sm"
                >Eroare prețuri + retry</CardTitle
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-3">
              <Skeleton className="h-32 w-full" />
              <Alert variant="destructive"
                ><AlertTitle>Nu am putut încărca prețurile</AlertTitle
                ><AlertDescription>Reîncearcă.</AlertDescription></Alert
              >
              <Button size="sm" variant="outline">Reîncearcă</Button>
            </CardContent>
          </Card>
        </div>

        <div class="grid gap-4 lg:grid-cols-2">
          <Card>
            <CardHeader
              ><CardTitle className="text-sm"
                >0 vendors — Not available</CardTitle
              ><CardDescription>empty state honest</CardDescription></CardHeader
            >
            <CardContent
              ><PriceComparison
                idSuffix="pc-empty"
                offers={[]}
                client:load
              /></CardContent
            >
          </Card>
          <Card>
            <CardHeader
              ><CardTitle className="text-sm">1/5 vendors</CardTitle
              ><CardDescription
                >single offer · cheapest highlight</CardDescription
              ></CardHeader
            >
            <CardContent
              ><PriceComparison
                idSuffix="pc-single"
                offers={PRICE_OFFERS_SINGLE}
                client:load
              /></CardContent
            >
          </Card>
        </div>

        <PriceComparison idSuffix="pc-default-4of5" client:load />

        <div class="grid gap-4 lg:grid-cols-2">
          <Card>
            <CardHeader
              ><CardTitle className="text-sm"
                >Large — 1.250.000 lei row</CardTitle
              ><CardDescription
                >ro-RO grouping stress</CardDescription
              ></CardHeader
            >
            <CardContent
              ><PriceComparison
                idSuffix="pc-large"
                offers={PRICE_OFFERS_LARGE}
                client:load
              /></CardContent
            >
          </Card>
          <Card>
            <CardHeader
              ><CardTitle className="text-sm">Variant qualifier</CardTitle
              ><CardDescription
                >24-ore / cantitativ badge</CardDescription
              ></CardHeader
            >
            <CardContent
              ><PriceComparison
                idSuffix="pc-variant"
                offers={PRICE_OFFERS_VARIANT}
                client:load
              /></CardContent
            >
          </Card>
        </div>

        <Card className="border-dashed">
          <CardHeader>
            <CardTitle className="text-sm"
              >N of M — 0/4 panel archetype (bundle) · gray</CardTitle
            >
            <CardDescription
              >Profil / bundle state — not yet sold as bundle here (never
              invent)</CardDescription
            >
          </CardHeader>
          <CardContent className="flex flex-col gap-3">
            <div class="rounded-lg border bg-muted/20 p-4">
              <div class="flex items-center justify-between gap-2">
                <span class="font-medium text-sm"
                  >Profil tiroidian — 0/4 analize disponibile</span
                >
                <Badge variant="secondary">0/4</Badge>
              </div>
              <p class="mt-2 text-muted-foreground text-xs leading-relaxed">
                Sante 0/4 · Synevo 1/4 · Invitro 0/4 — Not yet sold as a bundle
                here. Quorum ≥2, archetype referenceComponentIds honest.
              </p>
              <div class="mt-3 grid grid-cols-4 gap-2 text-xs">
                <div
                  class="rounded border bg-card px-2 py-2 text-center text-muted-foreground"
                >
                  TSH · —
                </div>
                <div
                  class="rounded border bg-card px-2 py-2 text-center text-muted-foreground"
                >
                  FT4 · —
                </div>
                <div
                  class="rounded border bg-card px-2 py-2 text-center text-muted-foreground"
                >
                  FT3 · —
                </div>
                <div
                  class="rounded border bg-card px-2 py-2 text-center text-muted-foreground"
                >
                  Anti-TPO · —
                </div>
              </div>
            </div>
            <p class="text-muted-foreground text-xs">
              Stare showcase W2: N of M 0/4 gray — honest null, nu inventăm
              bundle.
            </p>
          </CardContent>
        </Card>

        <Card className="border-dashed">
          <CardHeader>
            <CardTitle className="text-sm">EN — 270 MDL grouped</CardTitle
            ><CardDescription
              >270 lei (RO) / 270 MDL (EN) grouped check</CardDescription
            >
          </CardHeader>
          <CardContent
            ><PriceComparison
              idSuffix="pc-en"
              lang="en"
              priceAsOf="August 2026"
              client:load
            /></CardContent
          >
        </Card>
      </section>

      <section class="flex flex-col gap-4" id="showcase-product-price-real">
        <h2 class="font-heading text-sm font-semibold tracking-tight">
          Price wired — real getPricesForId('vitamina-b12') 4 vendors vs mock
          175
        </h2>
        <p class="text-muted-foreground text-xs leading-relaxed">
          Mock Sante 175 / Synevo 195 / Alfa 190 / Invitro 210 → Real Sante 166
          / Alfa 240 / Synevo 245 / Invitro 250 · MedExpert — Not available ·
          cheapest 166 fee-included (Sante 0 lei). 417 slugs now have real
          prices, 274 with ≥2 vendors; catalog_only honest [] no invent.
        </p>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card id="showcase-price-real-b12">
            <CardHeader>
              <CardTitle className="text-sm"
                >Real — vitamina-b12 166 lei</CardTitle
              >
              <CardDescription
                >getPricesForId + mapped to PriceOffer, priceAsOf august 2026,
                sourceUrl provenance</CardDescription
              >
            </CardHeader>
            <CardContent>
              <PriceComparison
                idSuffix="showcase-product-price-real"
                offers={PRICE_OFFERS_REAL_B12}
                priceAsOf="august 2026"
                client:load
              />
            </CardContent>
          </Card>
          <Card id="showcase-price-mock-vs-real">
            <CardHeader>
              <CardTitle className="text-sm">Mock vs Real table</CardTitle>
              <CardDescription
                >Shows honesty: Sante cheapest holds but value
                corrected</CardDescription
              >
            </CardHeader>
            <CardContent className="overflow-x-auto">
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>Lab</TableHead>
                    <TableHead className="text-right">Mock</TableHead>
                    <TableHead className="text-right">Real</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  <TableRow
                    ><TableCell>Sante</TableCell
                    ><TableCell className="text-right">175</TableCell
                    ><TableCell className="text-right font-medium text-primary"
                      >166</TableCell
                    ></TableRow
                  >
                  <TableRow
                    ><TableCell>Synevo</TableCell
                    ><TableCell className="text-right">195</TableCell
                    ><TableCell className="text-right">245</TableCell></TableRow
                  >
                  <TableRow
                    ><TableCell>Alfa</TableCell
                    ><TableCell className="text-right">190</TableCell
                    ><TableCell className="text-right">240</TableCell></TableRow
                  >
                  <TableRow
                    ><TableCell>Invitro</TableCell
                    ><TableCell className="text-right">210</TableCell
                    ><TableCell className="text-right">250</TableCell></TableRow
                  >
                  <TableRow className="bg-muted/10"
                    ><TableCell>MedExpert</TableCell
                    ><TableCell className="text-right">—</TableCell
                    ><TableCell className="text-right"
                      >— Not available</TableCell
                    ></TableRow
                  >
                </TableBody>
              </Table>
              <p class="mt-2 text-muted-foreground text-xs">
                Cheapest 166 lei (Sante, 0 lei fee) · plus 30 lei o dată
                Synevo/Invitro · 417 ids, 274 ≥2 vendors · sitemap 417 +
                MedicalTest JSON-LD per slug
              </p>
            </CardContent>
          </Card>
        </div>
        <Card className="overflow-hidden" id="showcase-price-real-empty">
          <CardHeader>
            <CardTitle className="text-sm"
              >Catalog only — honest empty []</CardTitle
            >
            <CardDescription
              >Quorum 0 → no price invented · PriceComparison shows
              Indisponibil</CardDescription
            >
          </CardHeader>
          <CardContent
            ><PriceComparison
              idSuffix="showcase-price-real-empty"
              offers={[]}
              priceAsOf="august 2026"
              client:load
            /></CardContent
          >
        </Card>
      </section>

      <Separator />

      <Faq idSuffix="showcase-faq" client:load />

      <Separator />

      <!-- W3 List matrix — soft 12, idSuffix isolated -->
      <section class="flex flex-col gap-4">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            List — soft 12 • Analizele mele / Mama control anual • share
            ?share=token &lt;2k
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            empty / one / many (12) / 13 soft non-blocking warning. anonToken
            localStorage, ulid, base64(JSON) token. Fiecare instanță idSuffix
            izolat, fără duplicate ARIA.
          </p>
        </div>
        <div class="grid gap-4 lg:grid-cols-2">
          <!-- empty -->
          <Card id="showcase-list-empty">
            <CardHeader
              ><CardTitle className="text-sm"
                >Empty — Analizele mele (0)</CardTitle
              ><CardDescription
                >id: 01LISTEMPTY • owner anonToken • 0/12</CardDescription
              ></CardHeader
            >
            <CardContent>
              <Empty className="border border-dashed">
                <EmptyHeader
                  ><EmptyMedia variant="icon">📋</EmptyMedia
                  ><EmptyTitle>No tests yet</EmptyTitle
                  ><EmptyDescription
                    >Add from search or bundle. Share link needs &lt;2k
                    token.</EmptyDescription
                  ></EmptyHeader
                >
              </Empty>
            </CardContent>
          </Card>
          <!-- one -->
          <Card id="showcase-list-one">
            <CardHeader
              ><CardTitle className="text-sm"
                >One — Mama control anual (1)</CardTitle
              ><CardDescription
                >id: 01LISTONE • 1/12 • pinnedBranch optional</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-2">
              <div
                class="flex items-center justify-between rounded-lg border bg-card px-3 py-2 text-sm"
                id="showcase-list-one-item"
              >
                <span>{TESTS[0]?.id ?? "tsh"}</span
                ><Badge variant="secondary">remove</Badge>
              </div>
              <div class="text-muted-foreground text-xs">
                Share:
                <code class="rounded bg-muted px-1 py-0.5 font-mono text-xs"
                  >?share=eyJ2IjoxLCJpZCI6IjAxIn0</code
                >
                base64 &lt;2k
              </div>
            </CardContent>
          </Card>
        </div>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card id="showcase-list-12">
            <CardHeader
              ><CardTitle className="text-sm"
                >Many — 12/12 soft limit • Analizele mele (12)</CardTitle
              ><CardDescription
                >Inline compară pe /liste/[id] — overflow-x-auto sticky Test
                col</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-2">
              <div class="grid grid-cols-3 gap-2">
                {TESTS.slice(0,12).map((t) => (
                  <div id={`showcase-12-${t.id}`} class="truncate rounded border bg-muted/20 px-2 py-1.5 text-xs">{t.id}</div>
                ))}
              </div>
              <div class="text-xs text-muted-foreground">
                12 tests • cheapestSingleLab Synevo total fee-included (plus 30
                lei o singură dată)
              </div>
            </CardContent>
          </Card>
          <Card className="border-amber-200" id="showcase-list-13">
            <CardHeader
              ><CardTitle className="text-sm"
                >13th — soft warning non-blocking</CardTitle
              ><CardDescription
                >13/12 • List mare — compararea poate fi lungă, împarte în
                două?</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-3">
              <div class="grid grid-cols-3 gap-2">
                {TESTS.slice(0,13).map((t) => (
                  <div id={`showcase-13-${t.id}`} class="truncate rounded border bg-muted/20 px-2 py-1.5 text-xs">{t.id}</div>
                ))}
              </div>
              <Alert
                ><AlertTitle>List mare</AlertTitle
                ><AlertDescription
                  >Compararea a 13+ teste poate fi lungă, împarte în două? —
                  warning, no hard throw. 13th still allowed.</AlertDescription
                ></Alert
              >
            </CardContent>
          </Card>
        </div>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card id="showcase-list-loading"
            ><CardHeader
              ><CardTitle className="text-sm"
                >Loading Skeleton</CardTitle
              ></CardHeader
            ><CardContent
              ><Skeleton className="h-24 w-full" /></CardContent
            ></Card
          >
          <Card className="border-destructive/50" id="showcase-list-error"
            ><CardHeader
              ><CardTitle className="text-sm text-destructive"
                >Error + retry</CardTitle
              ><CardDescription
                >Could not load lists</CardDescription
              ></CardHeader
            ><CardContent
              ><Button size="sm" variant="outline">Retry</Button></CardContent
            ></Card
          >
          <Card id="showcase-list-disabled"
            ><CardHeader
              ><CardTitle className="text-sm"
                >Disabled • share read-only</CardTitle
              ><CardDescription
                >?share token opens without account</CardDescription
              ></CardHeader
            ><CardContent
              ><Button disabled size="sm" variant="outline"
                >Share link read-only</Button
              ></CardContent
            ></Card
          >
        </div>
        <Card className="overflow-hidden" id="showcase-list-long">
          <CardHeader
            ><CardTitle className="text-sm"
              >Very long / unbreakable name</CardTitle
            ></CardHeader
          >
          <CardContent className="flex flex-col gap-2">
            <div class="break-all rounded border bg-card px-3 py-2 text-xs">
              SupercalifragilisticexpialidociousVitaminaB12CobalaminăTestDeSângeCuNumeFoarteLungSiFaraSpatiiCaSaTestamOverflow320px
            </div>
            <div class="truncate rounded border bg-card px-3 py-2 text-xs">
              Mama — control anual cu colesterol, trigliceride, hemoleucograma
              completă 5 diff și vitamina D 25-OH
            </div>
          </CardContent>
        </Card>
      </section>

      <Separator />

      <!-- W3 Comparison matrix — 1/12 tests, 12×5=60 cells, overflow-x-auto sticky -->
      <section class="flex flex-col gap-4">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            Comparison — soft 12 • inline on liste/[id] • standalone /comparatie
            • fee-included
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            ≤12 free, overflow-x-auto + sticky Test col. cheapestSingleLab
            &#123;labId,total&#125; fee-included + saveIfSplit without push.
          </p>
        </div>
        <!-- 1 test -->
        <Card id="showcase-compare-1">
          <CardHeader
            ><CardTitle className="text-sm">1 test — single row</CardTitle
            ><CardDescription
              >Cheapest single-lab: Synevo 260 lei total (fees included) • Save
              18 lei if split — without push</CardDescription
            ></CardHeader
          >
          <CardContent className="overflow-x-auto">
            <Table>
              <TableHeader
                ><TableRow
                  ><TableHead className="sticky left-0 bg-card px-3"
                    >Test</TableHead
                  ><TableHead className="px-3">Synevo</TableHead
                  ><TableHead className="px-3">Sante</TableHead
                  ><TableHead className="px-3">Invitro</TableHead
                  ><TableHead className="px-3">MedExpert</TableHead
                  ><TableHead className="px-3">Alfa</TableHead></TableRow
                ></TableHeader
              >
              <TableBody
                ><TableRow
                  ><TableCell className="sticky left-0 bg-card px-3 font-medium"
                    >{TESTS[3]?.id ?? "tsh"}</TableCell
                  ><TableCell className="px-3">120 lei</TableCell
                  ><TableCell className="px-3">110 lei</TableCell
                  ><TableCell className="px-3">135 lei</TableCell
                  ><TableCell className="px-3">—</TableCell
                  ><TableCell className="px-3">98 lei</TableCell></TableRow
                ></TableBody
              >
            </Table>
          </CardContent>
        </Card>
        <!-- 12 tests 60 cells -->
        <Card className="overflow-hidden" id="showcase-compare-12">
          <CardHeader
            ><CardTitle className="text-sm"
              >12 tests • 12×5=60 cells • overflow-x-auto sticky Test
              col</CardTitle
            ><CardDescription
              >Horizontal scroll, first col sticky. Count branches:
              {BRANCHES.length}
              • fee single line</CardDescription
            ></CardHeader
          >
          <CardContent className="overflow-x-auto">
            <Table>
              <TableHeader
                ><TableRow
                  ><TableHead className="sticky left-0 z-10 bg-muted/50 px-3"
                    >Test</TableHead
                  ><TableHead className="px-3">Synevo</TableHead
                  ><TableHead className="px-3">Sante</TableHead
                  ><TableHead className="px-3">Invitro</TableHead
                  ><TableHead className="px-3">MedExpert</TableHead
                  ><TableHead className="px-3">Alfa</TableHead></TableRow
                ></TableHeader
              >
              <TableBody>
                {TESTS.slice(0,12).map((t) => (
                  <TableRow key={t.id}><TableCell className="sticky left-0 z-10 bg-card px-3 font-medium">{t.id}</TableCell><TableCell className="px-3">120</TableCell><TableCell className="px-3">110</TableCell><TableCell className="px-3">135</TableCell><TableCell className="px-3">708</TableCell><TableCell className="px-3">—</TableCell></TableRow>
                ))}
              </TableBody>
            </Table>
          </CardContent>
        </Card>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card id="showcase-compare-loading"
            ><CardHeader
              ><CardTitle className="text-sm">Loading</CardTitle></CardHeader
            ><CardContent
              ><Skeleton className="h-24 w-full" /></CardContent
            ></Card
          >
          <Card className="border-destructive/50" id="showcase-compare-error"
            ><CardHeader
              ><CardTitle className="text-sm text-destructive"
                >Error Alert + retry</CardTitle
              ></CardHeader
            ><CardContent className="flex flex-col gap-2"
              ><Alert variant="destructive"
                ><AlertTitle>Could not load comparison</AlertTitle
                ><AlertDescription>Retry comparison.</AlertDescription></Alert
              ><Button size="sm" variant="outline">Retry</Button></CardContent
            ></Card
          >
          <Card className="border-dashed" id="showcase-compare-empty"
            ><CardHeader
              ><CardTitle className="text-sm">Empty comparison</CardTitle
              ><CardDescription
                >No tests selected — add 1–12</CardDescription
              ></CardHeader
            ><CardContent
              ><Empty
                ><EmptyHeader
                  ><EmptyTitle>No tests to compare</EmptyTitle
                  ><EmptyDescription
                    >Kindly widen — add from Analizele mele.</EmptyDescription
                  ></EmptyHeader
                ></Empty
              ></CardContent
            ></Card
          >
        </div>
        <Card id="showcase-compare-large-price">
          <CardHeader
            ><CardTitle className="text-sm"
              >Large price 1.250.000 lei + long name</CardTitle
            ></CardHeader
          >
          <CardContent className="flex flex-col gap-2"
            ><div class="break-all font-mono text-sm font-medium">
              1.250.000 lei
            </div>
            <div class="break-all text-xs text-muted-foreground">
              SupercalifragilisticexpialidociousTestCuNumeFoarteLungPentruOverflow320pxSi5LabsCuPreturiMari
            </div></CardContent
          >
        </Card>
        <Card id="showcase-branches">
          <CardHeader
            ><CardTitle className="text-sm"
              >Branches — 145 branches (10 Alfa + 33 Synevo + 25 Sante + 47
              Invitro + 30 MedExpert)</CardTitle
            ><CardDescription
              >Filter lab×sample×streetKey botanica/centru • Open now • phone •
              sampleTypes • geo lat/lng ·
              {BRANCHES.length}
              total</CardDescription
            ></CardHeader
          >
          <CardContent className="flex flex-col gap-2 text-xs">
            <div class="flex flex-wrap gap-2">
              <Badge>botanica</Badge><Badge variant="outline">centru</Badge
              ><Badge variant="secondary">Sânge</Badge
              ><Badge variant="outline">Urină</Badge>
              <Badge variant="outline">Open now</Badge>
            </div>
            <div class="text-muted-foreground">
              {BRANCHES.length}
              branches total •
              {BRANCHES.filter(b=>b.streetKey==="botanica").length}
              botanica •
              {BRANCHES.filter(b=>b.streetKey==="centru").length}
              centru •
              {BRANCHES.filter(b=>b.streetKey==="riscani").length}
              riscani •
              {BRANCHES.filter(b=>b.streetKey==="buiucani").length}
              buiucani
            </div>
            <div class="font-mono break-all">
              Example: {BRANCHES[0]?.address ?? "—"} •
              {BRANCHES[0]?.phone ?? "—"}
              •
              {BRANCHES[0]?.sampleTypes.join(", ")}
              •
              {`${BRANCHES[0]?.geo.lat.toFixed(3) ?? "—"},${BRANCHES[0]?.geo.lng.toFixed(3) ?? "—"}`}
              •
              {BRANCHES[0]?.labId ?? "—"}
            </div>
            <div class="text-muted-foreground">
              Phone format per lab, hours Mon–Sun parsed, streetKey derived from
              address, sampleTypes Sânge/Urină.
            </div>
          </CardContent>
        </Card>
      </section>

      <Separator />

      <!-- Branches — dedicated matrix (145 total) · lab×sample×streetKey · Open now · phone · sampleTypes · geo -->
      <section class="flex flex-col gap-4" id="showcase-branches-matrix">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            Branches — 145 total · lab×sample×streetKey · Open now · phone · geo
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            10 Alfa + 33 Synevo + 25 Sante + 47 Invitro + 30 MedExpert = 145.
            Filtre: lab, sampleTypes, streetKey
            (botanica/centru/riscani/buiucani/ciocana/telecentru), Open now din
            hours Mon–Sun. Fiecare instanță idSuffix izolat.
          </p>
        </div>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card id="showcase-branches-loading">
            <CardHeader
              ><CardTitle className="text-sm">Loading Skeleton</CardTitle
              ><CardDescription>branches loading</CardDescription></CardHeader
            >
            <CardContent><Skeleton className="h-24 w-full" /></CardContent>
          </Card>
          <Card className="border-destructive/50" id="showcase-branches-error">
            <CardHeader
              ><CardTitle className="text-sm text-destructive"
                >Error + retry</CardTitle
              ><CardDescription
                >Could not load branches</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-2"
              ><Alert variant="destructive"
                ><AlertTitle>Eroare la încărcarea filialelor</AlertTitle
                ><AlertDescription>Reîncearcă.</AlertDescription></Alert
              ><Button size="sm" variant="outline"
                >Reîncearcă</Button
              ></CardContent
            >
          </Card>
          <Card id="showcase-branches-empty">
            <CardHeader
              ><CardTitle className="text-sm"
                >Empty — 0 rezultate filtru</CardTitle
              ><CardDescription
                >lab×streetKey fără match</CardDescription
              ></CardHeader
            >
            <CardContent
              ><Empty className="border border-dashed"
                ><EmptyHeader
                  ><EmptyMedia variant="icon">📍</EmptyMedia
                  ><EmptyTitle>Nicio filială găsită</EmptyTitle
                  ><EmptyDescription
                    >Încearcă alt filtru — lab sau streetKey.</EmptyDescription
                  ></EmptyHeader
                ></Empty
              ></CardContent
            >
          </Card>
        </div>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card id="showcase-branches-one">
            <CardHeader
              ><CardTitle className="text-sm">One — 1 branch</CardTitle
              ><CardDescription
                >single result cu phone + geo</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-2 text-xs">
              <div class="rounded border bg-card px-3 py-2">
                <div class="font-medium">{BRANCHES[0]?.address ?? "—"}</div>
                <div class="text-muted-foreground">
                  {BRANCHES[0]?.labId ?? "—"}
                  • {BRANCHES[0]?.phone ?? "—"} •
                  {BRANCHES[0]?.sampleTypes.join(", ") ?? "—"}
                </div>
                <div class="font-mono">
                  {BRANCHES[0]?.geo.lat.toFixed(4) ?? "—"},
                  {BRANCHES[0]?.geo.lng.toFixed(4) ?? "—"}
                </div>
              </div>
            </CardContent>
          </Card>
          <Card id="showcase-branches-disabled">
            <CardHeader
              ><CardTitle className="text-sm"
                >Disabled • permission denied</CardTitle
              ><CardDescription>W4 missing state</CardDescription></CardHeader
            >
            <CardContent
              ><Button disabled size="sm" variant="outline"
                >Vezi pe hartă — disabled</Button
              ></CardContent
            >
          </Card>
        </div>
        <Card id="showcase-branches-many">
          <CardHeader
            ><CardTitle className="text-sm"
              >Many — 145 branches • primul 6</CardTitle
            ><CardDescription
              >overflow list + streetKey badges</CardDescription
            ></CardHeader
          >
          <CardContent className="flex flex-col gap-2">
            <div class="grid grid-cols-1 gap-2 sm:grid-cols-2">
              {BRANCHES.slice(0,6).map((b) => (
                <div id={`showcase-branches-many-${b.id}`} class="truncate rounded border bg-muted/20 px-2 py-1.5 text-xs">
                  <span class="font-medium">{b.labId}</span> • {b.streetKey} • {b.phone} • {b.geo.lat.toFixed(2)},{b.geo.lng.toFixed(2)}
                </div>
              ))}
            </div>
            <div class="text-xs text-muted-foreground">
              + {BRANCHES.length - 6} more • total {BRANCHES.length} • . . .
              streetKey derived, hours Mon–Sun parsed
            </div>
          </CardContent>
        </Card>
      </section>

      <Separator />

      <!-- B1 harta — lab×sample×streetKey filtered + count + Open now + kindly widen -->
      <section class="flex flex-col gap-4" id="showcase-harta">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            Harta — lab×sample×streetKey · 12 branches match · Open now · kindly
            widen
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            Filter lab×sample×streetKey
            (botanica/centru/riscani/buiucani/ciocana/telecentru per
            deriveStreetKey) · honest count · hours
            Record&lt;Weekday,string[]|null&gt; · phone · sampleTypes · geo
            lat/lng · hoursNote · empty kindly widen (ooux 04:52). idSuffix
            showcase-harta-* izolat.
          </p>
        </div>
        <div class="grid gap-4 lg:grid-cols-2">
          <Card id="showcase-harta-botanica">
            <CardHeader
              ><CardTitle className="text-sm"
                >Filtered — botanica ·
                {BRANCHES.filter((b) => b.streetKey === "botanica").length}
                branches match</CardTitle
              ><CardDescription
                >lab×streetKey botanica · honest count · phone ·
                geo</CardDescription
              ></CardHeader
            >
            <CardContent
              ><BranchesExplorer
                idSuffix="showcase-harta-botanica"
                initialLab="all"
                initialStreet="botanica"
                client:load
              /></CardContent
            >
          </Card>
          <Card id="showcase-harta-empty">
            <CardHeader
              ><CardTitle className="text-sm"
                >Empty — 0 match · kindly widen</CardTitle
              ><CardDescription
                >Frotiu → 0 (no branch has Frotiu) · ooux 04:52</CardDescription
              ></CardHeader
            >
            <CardContent
              ><LabsTeaser
                idSuffix="showcase-harta-empty"
                initialSample="Frotiu"
                initialStreet="telecentru"
                client:load
              /></CardContent
            >
          </Card>
        </div>
        <Card id="showcase-harta-320">
          <CardHeader
            ><CardTitle className="text-sm"
              >320px wrapper — harta many</CardTitle
            ><CardDescription
              >w-[320px] verified · no horizontal bleed</CardDescription
            ></CardHeader
          >
          <CardContent>
            <div class="mx-auto w-[320px] max-w-full">
              <BranchesExplorer idSuffix="showcase-harta-320" client:load />
            </div>
          </CardContent>
        </Card>
      </section>

      <Separator />

      <!-- Categorie — 6 concern landings calm entry before search -->
      <section class="flex flex-col gap-4" id="showcase-categorie">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            Categorie — 6 concern landings · energie · hormoni · inima · digestiv · inflamatie · diabet
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            Calm entry before search, Mito Browse by concern. Each page: 7 tests + 3 bundles via foldDiacritics title/slug/tuple. idSuffix showcase-categorie-* izolat.
          </p>
        </div>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card id="showcase-categorie-many">
            <CardHeader><CardTitle className="text-sm">Many — energie · 7 tests + 3 bundles</CardTitle><CardDescription>idSuffix showcase-categorie-many · foldDiacritics energie → Oboseală, somn, fier</CardDescription></CardHeader>
            <CardContent className="flex flex-col gap-2">
              <div class="flex flex-wrap gap-1.5">
                <Badge>Energie</Badge><Badge variant="outline">energie</Badge><Badge variant="secondary">Oboseală, somn, fier</Badge>
              </div>
              <div class="grid gap-1.5">
                {TESTS.slice(0, 7).map((t) => (
                  <div id={`showcase-categorie-many-${t.id}`} class="truncate rounded border bg-muted/20 px-2 py-1.5 text-xs">{t.id} · {t.tuple_key.slice(0, 32)}</div>
                ))}
              </div>
              <div class="text-xs text-muted-foreground">+ 3 bundles · /categorie/energie → 7 tests</div>
            </CardContent>
          </Card>
          <Card id="showcase-categorie-empty">
            <CardHeader><CardTitle className="text-sm">Empty — no match · kindly</CardTitle><CardDescription>idSuffix showcase-categorie-empty · filtered 0</CardDescription></CardHeader>
            <CardContent>
              <Empty className="border border-dashed"><EmptyHeader><EmptyMedia variant="icon">🔍</EmptyMedia><EmptyTitle>Nicio analiză</EmptyTitle><EmptyDescription>Încearcă alt concern — energie, hormoni, inima, digestiv, inflamatie, diabet.</EmptyDescription></EmptyHeader></Empty>
            </CardContent>
          </Card>
          <Card id="showcase-categorie-320">
            <CardHeader><CardTitle className="text-sm">320px wrapper</CardTitle><CardDescription>idSuffix showcase-categorie-320 · w-[320px] verified</CardDescription></CardHeader>
            <CardContent>
              <div class="mx-auto w-[320px] max-w-full rounded-xl border p-3">
                <div class="flex flex-col gap-2">
                  <Badge>Energie</Badge><span class="text-xs">Oboseală, somn, fier</span><a class="text-xs underline" href="/categorie/energie">/categorie/energie →</a>
                </div>
              </div>
            </CardContent>
          </Card>
        </div>
      </section>

      <Separator />

      <!-- PDP badge sample — Sânge/Urină/Frotiu + turnaround next to Test de sânge -->
      <section class="flex flex-col gap-4" id="showcase-pdp-badge">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            PDP badge — sampleType + turnaround · next to Test de sânge · before Adaugă în listă
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            Badge outline 12px tracking-wider uppercase · blood→Sânge, urine→Urină, frotiu→Frotiu · turnaround 1 zi / large · fee once footer unchanged
          </p>
        </div>
        <div class="grid gap-4 lg:grid-cols-3">
          <Card id="showcase-pdp-badge-blood">
            <CardHeader><CardTitle className="text-sm">Blood — Sânge · 1 zi</CardTitle><CardDescription>idSuffix showcase-pdp-badge-blood · specimen blood + turnaround 1 zi</CardDescription></CardHeader>
            <CardContent><ProductHeader idSuffix="showcase-pdp-badge-blood" name="Vitamina B12 (Cobalamină)" lowestPrice={166} sampleType="blood" turnaround="1 zi" client:load /></CardContent>
          </Card>
          <Card id="showcase-pdp-badge-urine">
            <CardHeader><CardTitle className="text-sm">Urine — Urină · 1 zi</CardTitle><CardDescription>idSuffix showcase-pdp-badge-urine · specimen urine + turnaround 1 zi</CardDescription></CardHeader>
            <CardContent><ProductHeader idSuffix="showcase-pdp-badge-urine" name="Urocultură" lowestPrice={120} sampleType="urine" turnaround="1 zi" client:load /></CardContent>
          </Card>
          <Card id="showcase-pdp-badge-frotiu-large">
            <CardHeader><CardTitle className="text-sm">Frotiu · 14 zile · large turnaround</CardTitle><CardDescription>idSuffix showcase-pdp-badge-frotiu-large · specimen frotiu + turnaround 14 zile (large)</CardDescription></CardHeader>
            <CardContent><ProductHeader idSuffix="showcase-pdp-badge-frotiu-large" name="Test Frotiu extins" lowestPrice={240} sampleType="frotiu" turnaround="14 zile" client:load /></CardContent>
          </Card>
        </div>
        <Card id="showcase-pdp-badge-null" className="border-dashed">
          <CardHeader><CardTitle className="text-sm">Null — no badge fallback</CardTitle><CardDescription>idSuffix showcase-pdp-badge-null · sampleType null, turnaround null → only Test de sânge</CardDescription></CardHeader>
          <CardContent><ProductHeader idSuffix="showcase-pdp-badge-null" name="Analiză fără probă" lowestPrice={null} sampleType={null} turnaround={null} client:load /></CardContent>
        </Card>
      </section>

      <Separator />

      <!-- Home matrix — append-only: HeroSearch + CatalogShelf + BundlesShelf + ConcernsRow + LabsTeaser -->
      <section class="flex flex-col gap-8" id="showcase-home">
        <div class="flex flex-col gap-1">
          <h2 class="font-heading text-sm font-semibold tracking-tight">
            Home — HeroSearch · CatalogShelf · BundlesShelf · ConcernsRow ·
            LabsTeaser
          </h2>
          <p class="text-muted-foreground text-xs leading-relaxed">
            Matrix side-by-side light+dark · 320px · idSuffix izolat · gap-6 ·
            semantic tokens · size vs h/w correct · Breadcrumb Laboratoare &gt;
            Botanica · Mito static map fallback
          </p>
        </div>

        <!-- HeroSearch -->
        <div class="flex flex-col gap-4">
          <h3 class="font-heading text-xs font-semibold tracking-tight">
            HeroSearch — loading / empty / one / many / error / disabled / long
            / variant / 320px + lg light/dark
          </h3>
          <div class="grid gap-4 lg:grid-cols-3">
            <Card id="showcase-home-hero-loading">
              <CardHeader
                ><CardTitle className="text-sm">Loading Skeleton h-12</CardTitle
                ><CardDescription
                  >idSuffix home-hero-loading</CardDescription
                ></CardHeader
              >
              <CardContent><Skeleton className="h-12 w-full" /></CardContent>
            </Card>
            <Card id="showcase-home-hero-empty">
              <CardHeader
                ><CardTitle className="text-sm">Empty — No match</CardTitle
                ><CardDescription
                  >CommandEmpty · idSuffix home-hero-empty</CardDescription
                ></CardHeader
              >
              <CardContent>
                <div class="rounded-xl border bg-card p-4">
                  <p class="text-sm">No match for “xyz”.</p>
                  <p class="text-muted-foreground text-xs">
                    Kindly widen — try tsh, feritina (without diacritics), or
                    glicemie≡glucoza.
                  </p>
                </div>
              </CardContent>
            </Card>
            <Card id="showcase-home-hero-error">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Error Alert destructive + Retry</CardTitle
                ><CardDescription
                  >idSuffix home-hero-error</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <Alert variant="destructive"
                  ><AlertTitle>Could not load search</AlertTitle
                  ><AlertDescription>Reîncearcă.</AlertDescription></Alert
                >
                <Button size="sm" variant="outline">Retry</Button>
              </CardContent>
            </Card>
          </div>

          <div class="grid gap-4 lg:grid-cols-2">
            <Card id="showcase-home-hero-one">
              <CardHeader
                ><CardTitle className="text-sm">One — 1 result</CardTitle
                ><CardDescription
                  >idSuffix home-hero-one</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <HeroSearch idSuffix="home-hero-one" client:load />
                <div class="rounded-xl border bg-card">
                  <ul class="p-1" role="listbox">
                    <li>
                      <a
                        class="flex min-w-0 items-center justify-between gap-2 rounded-lg px-3 py-2 text-sm hover:bg-muted"
                        href="/analize/tsh"
                        ><span class="truncate font-medium">TSH — Tiroida</span
                        ><span
                          class="break-all font-mono text-[11px] text-muted-foreground"
                          >tsh</span
                        ></a
                      >
                    </li>
                  </ul>
                </div>
              </CardContent>
            </Card>
            <Card id="showcase-home-hero-many">
              <CardHeader
                ><CardTitle className="text-sm">Many — 8 results</CardTitle
                ><CardDescription
                  >idSuffix home-hero-many · MiniSearch 8</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <HeroSearch idSuffix="home-hero-many" client:load />
                <div class="rounded-xl border bg-card">
                  <ul class="p-1" role="listbox">
                    {TESTS.slice(0,8).map((t) => (
                      <li><a class="flex min-w-0 items-center justify-between gap-2 rounded-lg px-3 py-2 text-sm hover:bg-muted" href={`/analize/${t.slug_ro ?? t.id}`}><span class="truncate font-medium">{t.name_ro ?? t.id}</span><span class="break-all font-mono text-[11px] text-muted-foreground">{t.slug_ro ?? t.id}</span></a></li>
                    ))}
                  </ul>
                </div>
              </CardContent>
            </Card>
          </div>

          <div class="grid gap-4 lg:grid-cols-3">
            <Card id="showcase-home-hero-disabled">
              <CardHeader
                ><CardTitle className="text-sm">Disabled</CardTitle
                ><CardDescription
                  >idSuffix home-hero-disabled · opacity 50</CardDescription
                ></CardHeader
              >
              <CardContent>
                <div class="opacity-50 pointer-events-none">
                  <HeroSearch idSuffix="home-hero-disabled" client:load />
                </div>
                <Button disabled className="mt-3" size="sm" variant="outline"
                  >Search — disabled</Button
                >
              </CardContent>
            </Card>
            <Card id="showcase-home-hero-long" className="overflow-hidden">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Long / unbreakable break-all slug + 1.250.000 lei</CardTitle
                ><CardDescription
                  >idSuffix home-hero-long</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <HeroSearch idSuffix="home-hero-long" client:load />
                <div
                  class="break-all rounded border bg-card px-3 py-2 text-xs font-mono"
                >
                  supercalifragilisticexpialidociousvitaminab12cobalaminacutantitativ24ore-foarte-lung-fara-spatii
                </div>
                <Badge variant="outline">1.250.000 lei</Badge>
                <div class="break-all text-xs text-muted-foreground">
                  1.250.000 lei — wrapping preț (ro-RO grouping)
                </div>
              </CardContent>
            </Card>
            <Card id="showcase-home-hero-variant">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Variant — feritina folded</CardTitle
                ><CardDescription
                  >idSuffix home-hero-variant · feritina →
                  Feritină</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <HeroSearch idSuffix="home-hero-variant" client:load />
                <div class="rounded border bg-muted/20 p-3 text-xs">
                  <span class="font-mono">feritina</span>
                  <span class="text-muted-foreground">→</span>
                  <span class="font-medium">Feritină</span>
                  <span class="text-muted-foreground">· foldDiacritics</span>
                  <div class="mt-2 break-all font-mono text-[11px]">
                    normalized: feritina
                  </div>
                </div>
              </CardContent>
            </Card>
          </div>

          <div class="grid gap-4 lg:grid-cols-2">
            <Card id="showcase-home-hero-320">
              <CardHeader
                ><CardTitle className="text-sm">320px wrapper</CardTitle
                ><CardDescription
                  >idSuffix home-hero-320 · 320px verified</CardDescription
                ></CardHeader
              >
              <CardContent>
                <div class="mx-auto w-[320px] max-w-full rounded-xl border p-3">
                  <HeroSearch idSuffix="home-hero-320" client:load />
                </div>
              </CardContent>
            </Card>
            <Card id="showcase-home-hero-lg">
              <CardHeader
                ><CardTitle className="text-sm">lg + dark preview</CardTitle
                ><CardDescription
                  >idSuffix home-hero-lg · light/dark</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <HeroSearch idSuffix="home-hero-lg" client:load />
                <div class="dark rounded-xl border bg-background p-3">
                  <p class="text-xs text-muted-foreground">
                    Dark preview — class dark on wrapper
                  </p>
                  <div class="mt-2 rounded-lg border bg-card p-3">
                    <span class="text-sm font-medium">TSH</span>
                    <span class="text-muted-foreground text-xs"
                      >· dark token</span
                    >
                  </div>
                </div>
              </CardContent>
            </Card>
          </div>
        </div>

        <Separator />

        <!-- CatalogShelf -->
        <div class="flex flex-col gap-4">
          <h3 class="font-heading text-xs font-semibold tracking-tight">
            CatalogShelf — loading 8 Skeleton AspectRatio · empty · error ·
            disabled · one · many 8 · long · missing image · large price
          </h3>
          <div class="grid gap-4 lg:grid-cols-3">
            <Card id="showcase-home-catalog-loading">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Loading — 8 Skeleton AspectRatio</CardTitle
                ><CardDescription
                  >idSuffix home-catalog-loading</CardDescription
                ></CardHeader
              >
              <CardContent
                ><CatalogShelf
                  idSuffix="home-catalog-loading"
                  loading={true}
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-catalog-empty">
              <CardHeader
                ><CardTitle className="text-sm">Empty — 0 tests</CardTitle
                ><CardDescription
                  >idSuffix home-catalog-empty</CardDescription
                ></CardHeader
              >
              <CardContent
                ><CatalogShelf
                  idSuffix="home-catalog-empty"
                  tests={[]}
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-catalog-error">
              <CardHeader
                ><CardTitle className="text-sm">Error Alert + Retry</CardTitle
                ><CardDescription
                  >idSuffix home-catalog-error</CardDescription
                ></CardHeader
              >
              <CardContent
                ><CatalogShelf
                  idSuffix="home-catalog-error"
                  error="Could not load tests"
                  client:load
                /></CardContent
              >
            </Card>
          </div>
          <div class="grid gap-4 lg:grid-cols-3">
            <Card id="showcase-home-catalog-one">
              <CardHeader
                ><CardTitle className="text-sm">One — 1 card</CardTitle
                ><CardDescription
                  >tests=[TESTS[0]] · idSuffix home-catalog-one</CardDescription
                ></CardHeader
              >
              <CardContent
                ><CatalogShelf
                  idSuffix="home-catalog-one"
                  tests={[TESTS[0]]}
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-catalog-disabled">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Disabled — button disabled</CardTitle
                ><CardDescription
                  >idSuffix home-catalog-disabled</CardDescription
                ></CardHeader
              >
              <CardContent
                ><CatalogShelf
                  idSuffix="home-catalog-disabled"
                  disabled={true}
                  tests={[TESTS[0]]}
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-catalog-many">
              <CardHeader
                ><CardTitle className="text-sm">Many — 8 cards</CardTitle
                ><CardDescription
                  >idSuffix home-catalog-many</CardDescription
                ></CardHeader
              >
              <CardContent
                ><CatalogShelf
                  idSuffix="home-catalog-many"
                  tests={TESTS.slice(0,8)}
                  client:load
                /></CardContent
              >
            </Card>
          </div>
          <Card id="showcase-home-catalog-long" className="overflow-hidden">
            <CardHeader
              ><CardTitle className="text-sm"
                >Long truncate vs break-all + missing image + large
                price</CardTitle
              ><CardDescription
                >idSuffix home-catalog-long</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-4">
              <CatalogShelf
                idSuffix="home-catalog-long"
                tests={[{...TESTS[0], name_ro: "SupercalifragilisticexpialidociousVitaminaB12CobalaminăTestDeSângeCuNumeFoarteLungSiFaraSpatiiPentruOverflow320px", slug_ro: "supercalifragilisticexpialidociousvitaminab12cobalaminacutantitativ24ore-foarte-lung-fara-spatii", oneSentenceWatcher: "SupercalifragilisticexpialidociousVitaminaB12CobalaminăTestDeSângeCuNumeFoarteLungSiFaraSpatiiPentruOverflow320px · break-all"}]}
                client:load
              />
              <div class="flex flex-wrap gap-2">
                <Badge>1.250.000 lei</Badge
                ><Badge variant="outline">missing image — No image</Badge
                ><Badge variant="secondary">truncate vs break-all</Badge>
              </div>
              <div class="flex gap-2">
                <div class="w-1/2">
                  <AspectRatio
                    ratio={4/3}
                    className="flex items-center justify-center border bg-muted rounded-lg"
                    ><span class="text-xs text-muted-foreground"
                      >No image</span
                    ></AspectRatio
                  >
                </div>
                <div class="w-1/2 break-all text-xs">
                  SupercalifragilisticexpialidociousVitaminaB12CobalaminăTestDeSângeCuNumeFoarteLungSiFaraSpatiiPentruOverflow320px
                  — 1.250.000 lei wrapping
                </div>
              </div>
            </CardContent>
          </Card>
          <Card id="showcase-home-catalog-large">
            <CardHeader
              ><CardTitle className="text-sm"
                >Large price 1.250.000 lei</CardTitle
              ><CardDescription
                >idSuffix home-catalog-large</CardDescription
              ></CardHeader
            >
            <CardContent className="flex flex-col gap-2">
              <div class="break-all font-mono text-sm font-medium">
                1.250.000 lei
              </div>
              <div class="break-all text-xs text-muted-foreground">
                Wrapping preț mare — ro-RO grouping verificat
              </div>
              <CatalogShelf
                idSuffix="home-catalog-large"
                tests={[TESTS[0], TESTS[1]]}
                client:load
              />
            </CardContent>
          </Card>
        </div>

        <Separator />

        <!-- BundlesShelf -->
        <div class="flex flex-col gap-4">
          <h3 class="font-heading text-xs font-semibold tracking-tight">
            BundlesShelf — many 2 real · empty 0/4 gray · long member list
          </h3>
          <Card id="showcase-home-bundles-many">
            <CardHeader
              ><CardTitle className="text-sm"
                >Many — 2 bundles real (33 archetypes, 2 preview)</CardTitle
              ><CardDescription
                >idSuffix home-bundles-many · PANEL_COMPARISON 33 panel
                archetypes quorum≥2 (36 market / 22 catalog_only) · honest 2
                preview, counts 4/4 vs Not yet sold</CardDescription
              ></CardHeader
            >
            <CardContent
              ><BundlesShelf
                idSuffix="home-bundles-many"
                client:load
              /></CardContent
            >
          </Card>
          <Card id="showcase-home-bundles-empty" className="border-dashed">
            <CardHeader
              ><CardTitle className="text-sm">Empty — 0/4 gray</CardTitle
              ><CardDescription
                >idSuffix home-bundles-empty · honest null</CardDescription
              ></CardHeader
            >
            <CardContent>
              <BundlesShelf
                idSuffix="home-bundles-empty"
                bundles={[{
                  id: "arch-bundle-empty-0-4",
                  name: "Profil demonstrativ — 0/4 analize disponibile (gray)",
                  members: ["TSH", "FT4", "FT3", "Anti-TPO"],
                  slug: "profil-empty",
                  watcher: "Honest gaps — not yet sold as bundle here",
                  whoFor: "Often for screening — compare labs plainly",
                  readNext: "0/4 archetype — gray muted, not invented.",
                  coverage: [
                    { lab: "Sante", covered: 0, total: 4, notSold: true },
                    { lab: "Synevo", covered: 0, total: 4, notSold: true },
                    { lab: "Invitro", covered: 1, total: 4, missing: ["FT3", "Anti-TPO"] },
                    { lab: "Alfa", covered: 0, total: 4, notSold: true },
                  ]
                }]}
                client:load
              />
            </CardContent>
          </Card>
          <Card id="showcase-home-bundles-long" className="overflow-hidden">
            <CardHeader
              ><CardTitle className="text-sm"
                >Long member list — break-all</CardTitle
              ><CardDescription
                >idSuffix home-bundles-long</CardDescription
              ></CardHeader
            >
            <CardContent>
              <BundlesShelf
                idSuffix="home-bundles-long"
                bundles={[{
                  id: "arch-long-members",
                  name: "Profil cu nume foarte lung pentru wrapping pe 320px și listă membri extensivă cu break-all și overflow controlat pe card",
                  members: ["SupercalifragilisticexpialidociousVitaminaB12CobalaminăTest", "Hemoleucogramă completă 5 diff cu formulă leucocitară extinsă", "Colesterol total + HDL + LDL + trigliceride + raporturi", "Vitamina D 25-OH cu variantă cantitativă și interpretare sezonieră"],
                  slug: "profil-long",
                  watcher: "Panel coverage — market archetype cu nume lung break-all pe 320px",
                  whoFor: "Often for broad screening — compare labs plainly cu membri mulți",
                  readNext: "Members join · break-words · gap-6 verified.",
                  coverage: [
                    { lab: "Sante", covered: 4, total: 4 },
                    { lab: "Synevo", covered: 2, total: 4, missing: ["Vitamina D", "Hemoleucograma"] },
                    { lab: "Invitro", covered: 0, total: 4, notSold: true },
                    { lab: "Alfa", covered: 0, total: 4, notSold: true },
                  ]
                }]}
                client:load
              />
              <div class="mt-3 break-all text-xs text-muted-foreground">
                SupercalifragilisticexpialidociousVitaminaB12CobalaminăTestDeSângeCuNumeFoarteLungSiFaraSpatiiCaSaTestamOverflow320px
              </div>
            </CardContent>
          </Card>
        </div>

        <Separator />

        <!-- ConcernsRow -->
        <div class="flex flex-col gap-4">
          <h3 class="font-heading text-xs font-semibold tracking-tight">
            ConcernsRow — many 6 · one selected · disabled
          </h3>
          <div class="grid gap-4 lg:grid-cols-3">
            <Card id="showcase-home-concerns-many">
              <CardHeader
                ><CardTitle className="text-sm">Many — 6 concerns</CardTitle
                ><CardDescription
                  >idSuffix home-concerns-many</CardDescription
                ></CardHeader
              >
              <CardContent
                ><ConcernsRow
                  idSuffix="home-concerns-many"
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-concerns-one">
              <CardHeader
                ><CardTitle className="text-sm">One selected — heart</CardTitle
                ><CardDescription
                  >idSuffix home-concerns-one · value heart</CardDescription
                ></CardHeader
              >
              <CardContent
                ><ConcernsRow
                  idSuffix="home-concerns-one"
                  value="heart"
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-concerns-disabled">
              <CardHeader
                ><CardTitle className="text-sm">Disabled</CardTitle
                ><CardDescription
                  >idSuffix home-concerns-disabled</CardDescription
                ></CardHeader
              >
              <CardContent>
                <div class="opacity-50 pointer-events-none">
                  <ConcernsRow idSuffix="home-concerns-disabled" client:load />
                </div>
                <p class="mt-2 text-xs text-muted-foreground">
                  ToggleGroup disabled — pointer-events-none + opacity-50
                </p>
              </CardContent>
            </Card>
          </div>
        </div>

        <Separator />

        <!-- LabsTeaser -->
        <div class="flex flex-col gap-4">
          <h3 class="font-heading text-xs font-semibold tracking-tight">
            LabsTeaser — many 12 · empty kindly widen · one · loading · error ·
            Open now vs Closed · large 145 · 320px + Breadcrumb
          </h3>
          <div class="grid gap-4 lg:grid-cols-2">
            <Card id="showcase-home-labs-many">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Many — 12 filtered (slice 0-12)</CardTitle
                ><CardDescription
                  >idSuffix home-labs-many · 12 match demo</CardDescription
                ></CardHeader
              >
              <CardContent
                ><LabsTeaser
                  idSuffix="home-labs-many"
                  branches={BRANCHES.slice(0,12)}
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-labs-empty">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Empty — kindly widen (Frotiu → 0)</CardTitle
                ><CardDescription
                  >idSuffix home-labs-empty · No branch matches</CardDescription
                ></CardHeader
              >
              <CardContent
                ><LabsTeaser
                  idSuffix="home-labs-empty"
                  initialSample="Frotiu"
                  client:load
                /></CardContent
              >
            </Card>
          </div>
          <div class="grid gap-4 lg:grid-cols-2">
            <Card id="showcase-home-labs-one">
              <CardHeader
                ><CardTitle className="text-sm">One — single branch</CardTitle
                ><CardDescription
                  >idSuffix home-labs-one · branches [0]</CardDescription
                ></CardHeader
              >
              <CardContent
                ><LabsTeaser
                  idSuffix="home-labs-one"
                  branches={[BRANCHES[0]]}
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-labs-loading">
              <CardHeader
                ><CardTitle className="text-sm">Loading Skeleton</CardTitle
                ><CardDescription
                  >idSuffix home-labs-loading</CardDescription
                ></CardHeader
              >
              <CardContent
                ><LabsTeaser
                  idSuffix="home-labs-loading"
                  loading={true}
                  client:load
                /></CardContent
              >
            </Card>
          </div>
          <div class="grid gap-4 lg:grid-cols-2">
            <Card id="showcase-home-labs-error">
              <CardHeader
                ><CardTitle className="text-sm">Error Alert + Retry</CardTitle
                ><CardDescription
                  >idSuffix home-labs-error</CardDescription
                ></CardHeader
              >
              <CardContent
                ><LabsTeaser
                  idSuffix="home-labs-error"
                  error="Could not load branches"
                  client:load
                /></CardContent
              >
            </Card>
            <Card id="showcase-home-labs-openclosed">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Open now vs Closed — Badge</CardTitle
                ><CardDescription
                  >idSuffix home-labs-openclosed · isOpenNow</CardDescription
                ></CardHeader
              >
              <CardContent className="flex flex-col gap-3">
                <LabsTeaser idSuffix="home-labs-openclosed" client:load />
                <div class="flex flex-wrap gap-2">
                  <Badge>Open now</Badge
                  ><Badge variant="outline">Closed · Opens at 8:00</Badge
                  ><Badge variant="secondary">145 total</Badge>
                </div>
              </CardContent>
            </Card>
          </div>
          <Card id="showcase-home-labs-large">
            <CardHeader
              ><CardTitle className="text-sm"
                >Large — 145 branches total</CardTitle
              ><CardDescription
                >idSuffix home-labs-large · grid many</CardDescription
              ></CardHeader
            >
            <CardContent
              ><LabsTeaser
                idSuffix="home-labs-large"
                client:load
              /></CardContent
            >
          </Card>
          <div class="grid gap-4 lg:grid-cols-2">
            <Card id="showcase-home-labs-320">
              <CardHeader
                ><CardTitle className="text-sm">320px wrapper</CardTitle
                ><CardDescription
                  >idSuffix home-labs-320 · w-[320px] verified</CardDescription
                ></CardHeader
              >
              <CardContent>
                <div class="mx-auto w-[320px] max-w-full">
                  <LabsTeaser idSuffix="home-labs-320" client:load />
                </div>
              </CardContent>
            </Card>
            <Card id="showcase-home-labs-breadcrumb">
              <CardHeader
                ><CardTitle className="text-sm"
                  >Breadcrumb — Laboratoare &gt; Botanica</CardTitle
                ><CardDescription
                  >idSuffix home-labs-breadcrumb · street botanica
                  filtered</CardDescription
                ></CardHeader
              >
              <CardContent
                ><LabsTeaser
                  idSuffix="home-labs-breadcrumb"
                  initialStreet="botanica"
                  client:load
                /></CardContent
              >
            </Card>
          </div>
          <Card id="showcase-home-labs-breadcrumb-lab">
            <CardHeader
              ><CardTitle className="text-sm"
                >Breadcrumb — Laboratoare &gt; Synevo &gt; Harta</CardTitle
              ><CardDescription
                >idSuffix home-labs-breadcrumb-lab · lab synevo
                filtered</CardDescription
              ></CardHeader
            >
            <CardContent
              ><LabsTeaser
                idSuffix="home-labs-breadcrumb-lab"
                initialLab="synevo"
                client:load
              /></CardContent
            >
          </Card>
          <div class="dark rounded-xl border bg-background p-4">
            <p class="text-xs text-muted-foreground">
              Dark preview — class dark wrapper around LabsTeaser
            </p>
            <div class="mt-3">
              <LabsTeaser idSuffix="home-labs-dark" client:load />
            </div>
          </div>
        </div>
      </section>

      <section class="mx-auto w-full max-w-7xl px-4 py-10 sm:px-6">
        <div class="flex flex-col gap-2">
          <h2 class="font-heading text-xl font-semibold tracking-tight">
            W3 — Missing islands (real) + error states
          </h2>
          <p class="text-sm text-muted-foreground">
            BranchesExplorer / AnalizeBrowse / BundleDetail — real mounts with
            loading / error+retry / disabled. Product islands now expose
            error/onRetry (verified via real prop, not wrapper).
          </p>
        </div>
        <div class="mt-6 grid gap-6">
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >ProductGallery — loading</CardTitle
              >
            </CardHeader>
            <CardContent>
              <ProductGallery
                idSuffix="showcase-gallery-loading"
                images={PRODUCT_GALLERY}
                loading
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >ProductGallery — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <ProductGallery
                idSuffix="showcase-gallery-error"
                images={PRODUCT_GALLERY}
                error="Nu am putut încărca imaginile"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >PriceComparison — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <PriceComparison
                idSuffix="showcase-price-error"
                error="Nu am putut încărca prețurile"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >ProductHeader — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <ProductHeader
                idSuffix="showcase-header-error"
                error="Nu am putut încărca datele"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >ProductSpecs — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <ProductSpecs
                idSuffix="showcase-specs-error"
                error="Nu am putut încărca specificațiile"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >BranchesExplorer — loading</CardTitle
              >
            </CardHeader>
            <CardContent>
              <BranchesExplorer
                idSuffix="showcase-branches-loading"
                loading
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >BranchesExplorer — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <BranchesExplorer
                idSuffix="showcase-branches-error"
                error="Nu am putut încărca filialele"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >BranchesExplorer — disabled</CardTitle
              >
            </CardHeader>
            <CardContent>
              <BranchesExplorer
                idSuffix="showcase-branches-disabled"
                disabled
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm">AnalizeBrowse — loading</CardTitle>
            </CardHeader>
            <CardContent>
              <AnalizeBrowse loading client:load />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >AnalizeBrowse — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <AnalizeBrowse
                error="Nu am putut încărca analizele"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >AnalizeBrowse — disabled</CardTitle
              >
            </CardHeader>
            <CardContent>
              <AnalizeBrowse disabled client:load />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm">BundleDetail — loading</CardTitle>
            </CardHeader>
            <CardContent>
              <BundleDetail
                id="arch-albumina"
                name="Albumina"
                referenceComponentIds={[
                  "proteina-totala",
                  "vitamina-b12",
                  "albumina",
                  "feritina",
                ]}
                vendors={["sante", "synevo"]}
                quorumSize={4}
                vendorCount={2}
                loading
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm"
                >BundleDetail — error + retry</CardTitle
              >
            </CardHeader>
            <CardContent>
              <BundleDetail
                id="arch-albumina"
                name="Albumina"
                referenceComponentIds={["albumina", "feritina"]}
                vendors={["sante"]}
                quorumSize={2}
                vendorCount={1}
                error="Nu am putut încărca pachetul"
                onRetry={() => {}}
                client:load
              />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <CardTitle className="text-sm">BundleDetail — disabled</CardTitle>
            </CardHeader>
            <CardContent>
              <BundleDetail
                id="arch-albumina"
                name="Albumina"
                referenceComponentIds={["albumina", "feritina"]}
                vendors={["sante"]}
                quorumSize={2}
                vendorCount={1}
                disabled
                client:load
              />
            </CardContent>
          </Card>
        </div>
      </section>

      <Separator />

      <Card className="border-dashed">
        <CardHeader>
          <CardTitle className="text-sm">Breakpoints & esențial</CardTitle>
          <CardDescription
            >Secțiuni păstrate: Hero 1:1, Header (lowest+provenance), Specs
            per-vendor onest, PriceComparison single fee, FAQ, Disclaimer.
            Șterse: Plan, Trust, Testimonials, HowItWorks, Interpretation,
            Related. Spacing S1=gap-3 S2=gap-6 S3=gap-8, PY=py-10, type Display
            3xl / Section 2xl / Body sm. Fee: 30 lei Synevo/Invitro, 25 Alfa, 0
            Sante/MedExpert — single line, nu per-row 10–60.</CardDescription
          >
        </CardHeader>
        <CardContent
          className="flex flex-wrap gap-2 text-xs text-muted-foreground"
        >
          <Badge variant="outline">320px verificat</Badge>
          <Badge variant="outline">light ✓</Badge>
          <Badge variant="secondary">minimal 5 secțiuni</Badge>
          <Badge>175 lei honest</Badge>
          <Badge variant="outline">vendor_fees.json</Badge>
        </CardContent>
      </Card>
    </main>

    <footer
      class="mx-auto max-w-7xl px-4 pb-8 pt-2 text-xs text-muted-foreground sm:px-6"
    >
      Showcase minimal —
      <code class="rounded bg-muted px-1 py-0.5 font-mono">@workspace/ui</code>
      + 1:1 + useState · W2 price truth · 7033 routes (417 comparison + 6616
      catalog_only) + 33 panels + 145 branches
    </footer>
  </body>
</html>
